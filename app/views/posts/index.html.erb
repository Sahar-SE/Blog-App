<div>
  <%= render 'users/user_card'%>

  <div>
    <%= link_to 'Add a new post', new_user_post_path %>
  </div>

  <% @posts.each do |post| %>
    <div class='post-card'>
      <h3><%=link_to post.title, user_post_path(@user.id, post), :class => 'post-title'%></h3>
      <p><%= post.text %></p>
      <div class='likes-comments'>
        <p>Comments: <%= post.commentsCounter.nil? ? 0 : post.commentsCounter %></p>
        <p>Likes: <%= post.likesCounter.nil? ? 0 : post.likesCounter %></p>
      </div>
    </div>
    <div class='comments_cnt'>
      <% post.recent_5_comments.each do |comment| %>
        <p class='comment'><%= comment.user.name %>: <%= comment.text %></p>
      <% end %>
    </div>
  <% end %>
   <button>Pagination</button>
</div>